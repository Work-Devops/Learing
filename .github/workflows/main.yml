name: My first github action
 on :
  
   push:
      branches:
        -main
  jobs:
    my_first_job:
       runs-on: ubuntu-latest
  steps :
      -name: checkout code
       uses: actions/checkout@v4
 -name: print the message 
  run:  echo "hi how are u"
       
